# Mock server only. This file is read by server/index.js, not by Vite.
# MOCK_WS_HOST omitted: server auto-selects the current local IPv4.
MOCK_WS_PORT=8787
MOCK_WS_PUSH_INTERVAL_MS=60000
MOCK_WS_FIRST_PUSH_DELAY_MS=2000
MOCK_WS_INCOMING_CALL_DELAY_MS=1200
