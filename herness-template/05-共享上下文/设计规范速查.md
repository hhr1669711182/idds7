# 设计规范速查

> 代码规范和设计约定。维护人：技术负责人。
> 项目落地时替换为实际技术栈的规范。

## 后端

### 架构规范
- {分层架构说明（如 Handler → Logic → Model）}
- {各层职责与禁改项}

### API 设计规范
- {路由定义方式}
- **{自动生成文件} 是类型定义的唯一源头**
- **🚫 禁止直接修改自动生成文件**（如 types/routes）：该文件由代码生成工具自动生成，手动修改会在下次生成时被覆盖。所有类型/路由变更必须走以下流程：
  1. 编辑对应的 API 定义文件
  2. 在项目目录下执行生成命令
  3. 该命令会重新生成全部相关文件

### 错误处理
- {错误返回方式}

### 日志规范
- {日志库与规范}

### 命名规范
- 文件名：{规则}
- 结构体：{规则}
- 函数/方法：{规则}
- 变量：{规则}

## 前端

### 组件规范
- {组件组织方式}
- 组件文件名：{规则}

### API 调用
- 所有 API 定义在 `{src/api}/` 下
- 使用封装的请求实例，不在组件中直接调用底层 HTTP 客户端

### 状态管理
- 全局状态使用 {状态管理库}
- 页面内状态使用 {响应式 API}

### 权限控制
- {权限控制方式}
