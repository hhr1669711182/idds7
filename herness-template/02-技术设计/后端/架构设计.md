# 后端架构设计

> 最后更新：待填写 | 维护人：后端负责人

## 1. 系统总览

{系统整体架构图，描述各服务、中间件、外部依赖的关系}

```
┌──────────────────────────────────────────────┐
│                  项目名                        │
│                                              │
│  ┌───────────┐  ┌─────────────┐              │
│  │ 前端       │  │ 服务A        │              │
│  │ ({框架})   │  │ ({职责})     │              │
│  └─────┬─────┘  └──────┬──────┘              │
│        │               │                     │
│        └───────┬───────┘                     │
│                │                             │
│         ┌──────▼──────┐    ┌────────────┐    │
│         │  数据库      │    │  缓存       │    │
│         └─────────────┘    └────────────┘    │
│                                              │
│  ┌──────────┐  ┌───────────┐  ┌──────────┐  │
│  │ 消息队列  │  │ 对象存储   │  │ 外部服务  │  │
│  └──────────┘  └───────────┘  └──────────┘  │
└──────────────────────────────────────────────┘
```

## 2. 服务启动流程

{服务启动流程，可用伪代码或流程图}

## 3. 分层架构

{分层架构说明（如 Handler → Logic → Model），各层职责与约束}

## 4. 依赖注入

{全局依赖容器说明：数据库连接、缓存、配置、中间件等共享资源的注入方式}

## 5. 中间件体系

{中间件列表、路由分组策略、鉴权方案}

## 6. 消息队列

{消息队列用途、Topic/Exchange 划分、生产与消费约定}

## 7. 外部依赖集成

| 服务 | 集成方式 | 说明 |
|------|---------|------|
| | | |

## 8. 目录结构

{源码目录结构说明}

## 9. 服务部署

{编译、部署、运行时依赖说明}

## 10. 设计原则与约定

{本项目后端设计原则，如：租户隔离、软删除、统一错误处理、日志规范、参数校验、缓存策略等}
