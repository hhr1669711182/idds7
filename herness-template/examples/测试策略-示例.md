# 迭代测试策略 — 示例：{YYYY-WXX}

> 本文档是迭代测试策略的示例，在迭代开始阶段产出，指导本迭代的测试工作。

---

## 1. 文档信息

| 项 | 值 |
|----|-----|
| 迭代 | {YYYY-WXX}（{MM-DD} ~ {MM-DD}） |
| 测试范围 | {功能范围} |
| 测试负责人 | <待指定> |

---

## 2. 测试目标

验证本次迭代功能：
1. {目标1}
2. {目标2}
3. {目标3}
4. 不影响现有 {已有功能} 流程

---

## 3. 测试范围与层级

| 层级 | 范围 | 负责人 | 工具 |
|------|------|--------|------|
| 单元测试 | {模块} logic、handler、model | 开发 | {测试框架} |
| 接口测试 | 新增 {N} 个 API | 测试 | {工具} |
| 集成测试 | {联动} + 数据库操作 | 测试 | {工具} |
| 回归测试 | 现有 {功能} | 测试 | {工具} |

---

## 4. 测试重点

### 4.1 重点模块

- **{模块1}**：{说明}（P0）
- **{模块2}**：{说明}（P0）

### 4.2 风险场景

| 风险 | 测试方法 |
|------|---------|
| {并发风险} | 并发测试 |
| {依赖故障} | Mock 故障，验证主流程不受影响 |
| {影响已有功能} | 回归测试 |

---

## 5. 测试环境

| 环境 | 用途 |
|------|------|
| 本地开发环境 | 单元测试 |
| 测试环境（test） | 接口测试 + 集成测试 |
| 预发布环境（staging） | 回归测试（上线前） |

---

## 6. 时间计划

| 阶段 | 时间 | 产出 |
|------|------|------|
| 测试策略 & 用例编写 | {日期} ~ {日期} | 迭代测试策略 + 测试用例文档 |
| 测试执行（功能） | {日期} ~ {日期} | 测试报告 |
| 回归测试 | {日期} ~ {日期} | 回归测试结果 |
