# 测试用例 — 示例：{模块}

> 本文档是功能模块测试用例的示例，演示完整的测试用例组织结构。

---

## 1. 文档信息

| 项 | 值 |
|----|-----|
| 模块 | {模块名} |
| 关联迭代 | {YYYY-WXX} |
| 关联技术方案 | [技术方案-{功能}](技术方案-示例.md) |

---

## 2. 用例概览

| 分类 | 用例数 |
|------|--------|
| 接口测试 — {子模块1} | 6 |
| 接口测试 — {子模块2} | 5 |
| 集成测试 — {联动} | 2 |
| 回归测试 — {已有功能} | 3 |
| **合计** | **N** |

---

## 3. 接口测试用例

### 3.1 {接口说明}（POST /api/v1/{module}/{action}）

| # | 用例名称 | 前置条件 | 步骤 | 期望结果 | 优先级 |
|---|---------|---------|------|---------|--------|
| 1 | 正常创建 | 对象存在 | POST count=10 | HTTP 200, 返回 10 条 | P0 |
| 2 | 边界值 | 对象存在 | POST count=1 | HTTP 200, 返回 1 条 | P1 |
| 3 | count=0（异常边界） | — | POST count=0 | HTTP 400, "{提示}" | P1 |
| 4 | 对象不存在 | — | POST key="NOT_EXIST" | HTTP 404, "{提示}" | P0 |

---

## 4. 集成测试用例

### 4.1 消息队列同步

| # | 用例名称 | 步骤 | 期望结果 |
|---|---------|------|---------|
| 1 | 创建时发送消息 | 创建 10 个 | 消息队列收到对应事件 |

### 4.2 异常场景

| # | 用例名称 | 步骤 | 期望结果 |
|---|---------|------|---------|
| 1 | 消息队列不可用 | Mock 故障，创建 | 创建成功，不影响主流程 |

---

## 5. 回归测试用例

| # | 用例名称 | 步骤 | 期望结果 |
|---|---------|------|---------|
| 1 | 已有功能不受影响 | 使用原流程 | 正常，不走新逻辑 |
