# 技术方案 — 示例：{功能} 方案

> 本文档是技术方案设计文档的示例，演示技术方案的完整结构。

---

## 1. 文档信息

| 项 | 值 |
|----|-----|
| 关联迭代 | {YYYY-WXX} |
| 关联 PRD | [PRD-{功能}](../../01-产品设计/开发中/PRD-{功能}.md) |
| 关联需求分析 | [需求分析-{功能}](需求分析-示例.md) |
| 作者 | {作者} |
| 日期 | {YYYY-MM-DD} |

---

## 2. 方案概述

**目标**：{一句话描述方案要解决什么问题}。

**涉及范围**：
- 新增 {N} 张数据表
- 新增 {N} 个 API
- 改造 {N} 个现有模块
- 新增/调整外部同步逻辑

---

## 3. 数据库变更

### 3.1 新增表：{table_name}

```sql
CREATE TABLE {table_name} (
    id           BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    {field}      VARCHAR(64) NOT NULL COMMENT '{说明}',
    created_at   DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_{field} ({field})
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='{表说明}';
```

**设计要点**：
- {要点1，如：使用后 DELETE，不设状态字段}
- {要点2，如：软删除策略}
- {要点3，如：唯一键设计}

### 3.2 无需变更的表

- {table}：不改表结构，仅在 API 返回中增加字段（运行时计算）

---

## 4. API 设计

### 4.1 {接口说明}

```
POST /api/v1/{module}/{action}
```

**请求**：
```json
{
    "key": "X1",
    "count": 10
}
```

**响应**：
```json
{
    "code": 0,
    "data": {
        "items": []
    }
}
```

### 4.2 {接口说明}

```
GET /api/v1/{module}/list?key=X1&page=1&page_size=20
```

---

## 5. 业务逻辑

### 5.1 {核心流程}

```
1. 校验参数
2. 查询/创建数据
3. 发送事件到消息队列
4. 返回结果
```

### 5.2 {改造流程}

```
改造前：{原逻辑}
改造后：
  1. {步骤1}
  2. {步骤2}
  3. {步骤3}
```

---

## 6. 涉及文件清单

### 新增文件
| 文件 | 说明 |
|------|------|
| `{路径}/model/{name}.go` | Model |
| `{路径}/handler/{name}.go` | Handler |
| `{路径}/logic/{name}.go` | Logic |

### 修改文件
| 文件 | 修改点 |
|------|--------|
| `{路径}/logic/{name}.go` | {修改点说明} |
| `{路径}/routes/routes.go` | 注册新 API 路由 |

---

## 7. 依赖与风险

| 依赖/风险 | 说明 | 应对 |
|----------|------|------|
| {依赖} | {说明} | {应对措施} |
| {并发风险} | {说明} | 使用行锁/事务 |
